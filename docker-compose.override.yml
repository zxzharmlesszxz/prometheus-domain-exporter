services:
  exporter:
    command:
      - --web.listen-address=:${COMPOSE_EXPORTER_PORT:-9853}
      - --${FEATURE_NAME}.refresh-interval=30s
      - --${FEATURE_NAME}.domain=google.com
      - --${FEATURE_NAME}.domain=example.com
      - --${FEATURE_NAME}.domain=auth-protection.services

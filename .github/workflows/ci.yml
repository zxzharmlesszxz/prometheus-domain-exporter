---

name: CI

on:
  push:
    branches:
      - "**"
    tags:
      - "**"
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

jobs:
  ci:
    uses: zxzharmlesszxz/prometheus-exporter-framework/.github/workflows/exporter-ci.yml@v0.5.0
    with:
      project-name: prometheus-domain-exporter
      go-version: "1.27.0"
      docker-platforms: "linux/amd64,linux/arm64"
    secrets: inherit
